<Window x:Class="InvoiceCreator.View.AddressBookView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InvoiceCreator.View"
        mc:Ignorable="d"
        Title="AddressBookView" Height="450" Width="710">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition ></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid  Grid.Row="0" IsReadOnly="True" AutoGenerateColumns="False"  ItemsSource="{Binding Customers}"
                  RowHeaderWidth="0" CanUserAddRows="False" CanUserDeleteRows="False" VerticalScrollBarVisibility="Visible">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="3*"/>
                <DataGridTextColumn Binding="{Binding NIP}" Header="NIP" Width="2*"/>
                <DataGridTextColumn Binding="{Binding AddressInline}" Header="Address" Width="5*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1">
            <Button Content="test" Width="100" Margin="10"></Button>
        </StackPanel>

    </Grid>
</Window>
